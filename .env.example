# SecureDoc AI Environment Variables

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=securedoc_db
DB_PORT=3306

# Secret key for application security
SECRET_KEY=securedoc_ai_super_secret_key_2026

# Temporary Directory Path (Optional, defaults to system temp)
TEMP_DIR=./temp_files
